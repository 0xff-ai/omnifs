name: omnifs-just
description: Install pinned maintainer command tools for omnifs CI
runs:
  using: composite
  steps:
    - uses: taiki-e/install-action@v2
      with:
        tool: just@1.54.0

    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.3.10
