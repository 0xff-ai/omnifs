{
  "provider": "kubernetes",
  "mount": "k8s",
  "config": {
    "endpoint": "unix:///run/omnifs/k8s.sock"
  },
  "capabilities": {
    "unix_sockets": {
      "dynamic": true
    }
  },
  "limits": {
    "max_memory_mb": 256
  }
}
