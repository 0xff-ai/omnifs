# DNS dev mount

Included in the `default` and `full` dev profiles. No auth or fixture; resolves over DNS-over-HTTPS to the bundled public resolvers (capability grants seeded from the provider manifest).
