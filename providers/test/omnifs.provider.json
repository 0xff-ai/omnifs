{
  "id": "test-provider",
  "displayName": "A test provider with canned data",
  "color": "#6B7280",
  "provider": "test_provider.wasm",
  "defaultMount": "test",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "root_ignore": {
        "type": "boolean",
        "default": false,
        "description": "Expose a provider-backed mount-root .gitignore for FUSE regression tests."
      }
    }
  },
  "capabilities": [
    {
      "kind": "domain",
      "value": "httpbin.org",
      "why": "Synthetic fixture endpoint exercised by host guest-loader tests."
    },
    {
      "kind": "memoryMb",
      "value": 16,
      "why": "Canned-data fixture needs only a small heap."
    }
  ]
}
