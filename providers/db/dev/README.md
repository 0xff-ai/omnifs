# DB dev mount

Chinook SQLite fixture for `omnifs dev --profile full`. The fixture image is built from `Dockerfile` and seeds `~/.omnifs/dev/fixtures/db/test.db` before the runtime container starts.
