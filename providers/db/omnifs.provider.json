{
  "id": "db",
  "displayName": "Database",
  "provider": "omnifs_provider_db.wasm",
  "defaultMount": "db",
  "capabilities": [
    {
      "kind": "memoryMb",
      "value": 128,
      "why": "Leave room for SQLite schema inspection and bounded sample result encoding."
    },
    {
      "kind": "preopenedPath",
      "value": { "host": "configured database file directory", "guest": "configured database file directory", "mode": "ro" },
      "dynamic": true,
      "why": "Expose the configured database file to the sandbox as a read-only WASI preopen, resolved from the `path` config field at mount-start."
    }
  ],
  "configSchema": {
    "type": "object",
    "required": ["path"],
    "additionalProperties": false,
    "properties": {
      "path": {
        "type": "string",
        "default": "/data/test.db",
        "description": "SQLite database file",
        "x-omnifs-resource": { "kind": "file", "mode": "ro" }
      },
      "read_only": {
        "type": "boolean",
        "default": true,
        "description": "Open the database read-only"
      },
      "sample_limit": {
        "type": "integer",
        "minimum": 1,
        "default": 20,
        "description": "Maximum sample rows per table"
      }
    }
  }
}
