[Content]
# Dev-only convenience, not a production credential: `just guest-image`
# defaults to this profile, and the resulting image is a local build artifact
# under target/ (gitignored) that is never published. An unlocked,
# autologin-enabled root shell on the serial console is how the boot smoke
# (`just guest-image-smoke`) and manual debugging inspect a guest that gets
# stuck.
RootPassword=hashed:
Autologin=yes
