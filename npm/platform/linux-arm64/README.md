# @0xff-ai/omnifs-cli-linux-arm64

Native `omnifs` host CLI plus `omnifs-fuse` and `omnifs-nfs` frontend runners
for Linux arm64. This package is installed as an optional dependency of the
root `omnifs` npm package.
