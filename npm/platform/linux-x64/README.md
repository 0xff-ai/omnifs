# @0xff-ai/omnifs-cli-linux-x64

Native `omnifs` host CLI plus `omnifs-fuse` and `omnifs-nfs` frontend runners
for Linux x64. This package is installed as an optional dependency of the root
`omnifs` npm package.
