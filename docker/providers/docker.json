{
  "plugin": "omnifs_provider_docker.wasm",
  "mount": "docker",
  "capabilities": {
    "unix_sockets": ["/var/run/docker.sock"],
    "max_memory_mb": 64
  },
  "config": {
    "endpoint": "unix:///var/run/docker.sock"
  }
}
